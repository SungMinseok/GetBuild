# 빌드 및 배포 시스템 구조 문서

## 📋 목차
1. [시스템 개요](#시스템-개요)
2. [build.py 상세 분석](#buildpy-상세-분석)
3. [deploy_local.py 상세 분석](#deploy_localpy-상세-분석)
4. [버전 관리 시스템](#버전-관리-시스템)
5. [통합 워크플로우](#통합-워크플로우)
6. [다른 프로젝트 적용 가이드](#다른-프로젝트-적용-가이드)

---

## 시스템 개요

### 목적
- Python 애플리케이션을 EXE 파일로 빌드
- Semantic Versioning (SemVer) 기반 버전 관리
- GitHub Release를 통한 자동 배포
- 사용자 친화적인 업데이트 시스템

### 핵심 컴포넌트
```
프로젝트 루트/
├── build.py              # EXE 빌드 및 버전 관리
├── deploy_local.py       # GitHub Release 배포
├── update_version.py     # 버전 업데이트 전용 스크립트
├── deploy.bat            # Windows 전체 자동화 배치
├── version.json          # 버전 정보 저장
└── changelog.txt         # 릴리즈 노트 (배포 시 생성)
```

### 기술 스택
- **빌드**: PyInstaller
- **버전 관리**: Semantic Versioning (MAJOR.MINOR.PATCH)
- **배포**: GitHub REST API
- **알림**: Slack Webhook (선택사항)

---

## build.py 상세 분석

### 1. 핵심 기능

#### 1.1 버전 관리 (Semantic Versioning)
```python
def parse_semver(version_str):
    """SemVer 문자열을 파싱하여 (major, minor, patch) 반환"""
    try:
        parts = version_str.split('.')
        if len(parts) >= 3:
            return int(parts[0]), int(parts[1]), int(parts[2])
        return 1, 0, 0
    except:
        return 1, 0, 0
```

**동작 원리:**
- 현재 버전을 `version.json`에서 읽어옴
- 버전 타입(PATCH/MINOR/MAJOR)에 따라 자동 증가
- 새 버전을 `version.json`에 저장

**버전 규칙:**
| 타입 | 설명 | 예시 |
|------|------|------|
| PATCH | 버그 수정 | 1.0.0 → 1.0.1 |
| MINOR | 새 기능 추가 (하위 호환) | 1.0.1 → 1.1.0 |
| MAJOR | Breaking changes | 1.1.0 → 2.0.0 |

#### 1.2 버전 업데이트 함수
```python
def update_version(version_type='patch', changelog_message=None):
    """
    version.json을 Semantic Versioning 방식으로 업데이트
    
    Args:
        version_type: 'major', 'minor', 'patch' 중 하나
        changelog_message: 변경사항 메시지
    """
    build_date = datetime.now().strftime("%Y-%m-%d")
    
    # 기존 버전 로드
    if os.path.exists('version.json'):
        with open('version.json', 'r', encoding='utf-8') as f:
            version_data = json.load(f)
        current_version = version_data.get('version', '1.0.0')
    else:
        version_data = {}
        current_version = "1.0.0"
    
    # 버전 파싱 및 증가
    major, minor, patch = parse_semver(current_version)
    
    if version_type == 'major':
        major += 1
        minor = 0
        patch = 0
    elif version_type == 'minor':
        minor += 1
        patch = 0
    else:  # patch
        patch += 1
    
    new_version = f"{major}.{minor}.{patch}"
    
    # version.json 업데이트
    version_data['version'] = new_version
    version_data['build_date'] = build_date
    
    # changelog 추가
    if changelog_message:
        new_changelog = {
            "version": new_version,
            "date": build_date,
            "changes": [changelog_message]
        }
        if 'changelog' not in version_data:
            version_data['changelog'] = []
        version_data['changelog'].insert(0, new_changelog)
    
    # 파일 저장
    with open('version.json', 'w', encoding='utf-8') as f:
        json.dump(version_data, f, ensure_ascii=False, indent=2)
    
    return new_version
```

#### 1.3 Windows 버전 파일 생성
```python
def create_version_file():
    """Windows EXE에 포함될 버전 정보 파일 생성"""
    version_info = load_version_info()
    version = version_info.get('version', '1.0.0')
    
    # SemVer를 Windows 버전 형식으로 변환
    # MAJOR.MINOR.PATCH → MAJOR,MINOR,PATCH,0
    version_parts = version.split('.')
    while len(version_parts) < 4:
        version_parts.append('0')
    
    file_version = ','.join(version_parts[:4])
    
    # version_info.txt 파일 생성 (PyInstaller용)
    version_file_content = f"""
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=({file_version}),
    prodvers=({file_version}),
    ...
  ),
  kids=[
    StringFileInfo([
      StringTable(u'040904B0', [
        StringStruct(u'FileVersion', u'{version}'),
        StringStruct(u'ProductVersion', u'{version}'),
        ...
      ])
    ]),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
"""
    with open('version_info.txt', 'w', encoding='utf-8') as f:
        f.write(version_file_content)
    
    return 'version_info.txt'
```

#### 1.4 PyInstaller Spec 파일 동적 생성
```python
def create_spec_file():
    """PyInstaller spec 파일을 동적으로 생성"""
    # 포함할 데이터 파일 목록
    datas_list = ["('version.json', '.')"]
    
    for folder in ['bundles', 'preset', 'design']:
        if os.path.exists(folder):
            datas_list.append(f"('{folder}', '{folder}')")
    
    spec_content = f"""
# -*- mode: python ; coding: utf-8 -*-
block_cipher = None

a = Analysis(
    ['main.py'],
    pathex=[os.getcwd()],
    binaries=[],
    datas=[
        {',\n        '.join(datas_list)}
    ],
    hiddenimports=[
        'PyQt5.QtCore',
        'PyQt5.QtGui',
        'PyQt5.QtWidgets',
        ...
    ],
    ...
)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='BundleEditor',
    debug=False,
    console=False,
    version='version_info.txt',
    icon='probe.ico',
)
"""
    with open('PbbAuto.spec', 'w', encoding='utf-8') as f:
        f.write(spec_content)
    
    return 'PbbAuto.spec'
```

#### 1.5 EXE 빌드
```python
def build_exe(spec_file):
    """PyInstaller로 EXE 빌드"""
    exe_path = 'dist/BundleEditor.exe'
    
    # 이미 존재하면 건너뛰기
    if os.path.exists(exe_path):
        print(f"[SKIP] EXE already exists: {exe_path}")
        return True
    
    # 빌드 폴더 정리
    for folder in ['build', 'dist']:
        if os.path.exists(folder):
            shutil.rmtree(folder, ignore_errors=True)
    
    # PyInstaller 실행
    cmd = [sys.executable, '-m', 'PyInstaller', '--clean', spec_file]
    result = subprocess.run(cmd, check=False)
    
    if result.returncode != 0:
        raise Exception(f"PyInstaller failed with code {result.returncode}")
    
    # EXE 생성 확인
    if not os.path.exists(exe_path):
        raise Exception(f"EXE file not found at: {exe_path}")
    
    return True
```

### 2. 실행 흐름 (main 함수)

```python
def main():
    # 환경변수 확인 (deploy.bat 사용 시 버전 업데이트 건너뛰기)
    skip_version_update = os.environ.get('SKIP_VERSION_UPDATE', '0') == '1'
    
    if not skip_version_update:
        # 1. 버전 타입 선택
        print("버전 업데이트 타입을 선택하세요:")
        print("  1. PATCH (버그 수정)")
        print("  2. MINOR (새 기능 추가)")
        print("  3. MAJOR (Breaking changes)")
        version_choice = input("선택: ").strip()
        
        version_type_map = {'1': 'patch', '2': 'minor', '3': 'major', '': 'patch'}
        version_type = version_type_map.get(version_choice, 'patch')
        
        # 2. 변경사항 입력
        changelog_msg = input("변경사항: ").strip() or "Auto Build"
        
        # 3. 버전 업데이트
        new_version = update_version(version_type, changelog_msg)
    else:
        # 버전 업데이트 건너뛰기 (deploy.bat 사용 시)
        version_info = load_version_info()
        new_version = version_info.get('version', '1.0.0')
    
    # 4. Windows 버전 파일 생성
    create_version_file()
    
    # 5. Spec 파일 생성
    spec_file = create_spec_file()
    
    # 6. EXE 빌드
    if not build_exe(spec_file):
        sys.exit(1)
    
    # 7. 임시 파일 정리
    clean_build()
    
    print(f"\n✅ 빌드 완료! 버전: {new_version}")
```

### 3. 주요 특징

#### 3.1 환경변수 지원
- `SKIP_VERSION_UPDATE=1`: 버전 업데이트 건너뛰기
- `deploy.bat`에서 사용하여 중복 버전 업데이트 방지

#### 3.2 오류 처리
```python
try:
    # 빌드 작업
    if not build_exe(spec_file):
        sys.exit(1)
except Exception as e:
    print(f"[ERROR] 빌드 실패: {e}")
    import traceback
    traceback.print_exc()
    sys.exit(1)
```

#### 3.3 사용자 친화적 UI
```
========================================
PbbAuto Build Script (Semantic Versioning)
========================================

버전 업데이트 타입을 선택하세요:
  1. PATCH (버그 수정) - 기본값
  2. MINOR (새 기능 추가)
  3. MAJOR (Breaking changes)
선택 (1/2/3, Enter=1): 2

변경사항을 입력하세요 (Enter만 누르면 'Auto Build' 사용):
> 자동 저장 기능 추가

[1/5] Updating version.json...
[INFO] 이전 버전: 1.0.0
🟡 MINOR 버전 업데이트 (New features)
[INFO] 새 버전: 1.1.0
[DONE] version.json 업데이트 완료!

[2/5] Creating version file...
[3/5] Creating spec file...
[4/5] Building EXE...
[5/5] Cleaning up...

========================================
Build completed successfully!
========================================
Version: 1.1.0
Changelog: 자동 저장 기능 추가
```

---

## deploy_local.py 상세 분석

### 1. 핵심 기능

#### 1.1 ZIP 패키지 생성
```python
def create_zip_package():
    """빌드된 EXE와 version.json을 ZIP으로 패키징"""
    dist_dir = Path("dist")
    exe_file = dist_dir / "BundleEditor.exe"
    version_json_path = Path('version.json')
    zip_path = dist_dir / "BundleEditor.zip"
    
    # 파일 존재 확인
    if not exe_file.exists():
        print(f"❌ EXE 파일을 찾을 수 없습니다: {exe_file}")
        return None
    
    if not version_json_path.exists():
        print(f"❌ version.json 파일을 찾을 수 없습니다")
        return None
    
    # 기존 ZIP 삭제
    if zip_path.exists():
        zip_path.unlink()
    
    # ZIP 생성
    with zipfile.ZipFile(zip_path, 'w', compression=zipfile.ZIP_DEFLATED) as zipf:
        zipf.write(exe_file, 'BundleEditor.exe')
        zipf.write(version_json_path, 'version.json')
    
    print(f"✅ ZIP 패키지 생성 완료: {zip_path}")
    return zip_path
```

**포함 파일:**
- `BundleEditor.exe`: 빌드된 실행 파일
- `version.json`: 버전 정보 (자동 업데이트 시스템용)

#### 1.2 GitHub 토큰 관리
```python
def get_github_token():
    """GitHub 토큰 가져오기"""
    # 방법 1: token.json 파일에서 읽기
    token_path = Path("token.json")
    if token_path.exists():
        with open(token_path, "r", encoding="utf-8") as f:
            token_data = json.load(f)
            if "github_token" in token_data:
                return token_data["github_token"]
    
    # 방법 2: 환경변수에서 읽기
    token = os.environ.get("GITHUB_TOKEN")
    if token:
        return token
    
    print("GitHub 토큰을 찾을 수 없습니다.")
    return None
```

**token.json 형식:**
```json
{
  "github_token": "ghp_xxxxxxxxxxxxxxxxxxxx",
  "webhook_team1": "https://hooks.slack.com/services/...",
  "webhook_team2": "https://hooks.slack.com/services/..."
}
```

#### 1.3 GitHub Release 생성
```python
def create_github_release(version, changelog, token, zip_path):
    """GitHub 릴리즈 생성 및 파일 업로드"""
    repo_owner = "SungMinseok"
    repo_name = "PbbAuto"
    tag_name = version  # 예: "1.1.0"
    
    # 1. changelog.txt 파일 처리
    changelog_file_path = "changelog.txt"
    
    # 파일이 없으면 자동으로 열어서 사용자가 작성하도록 함
    if not os.path.exists(changelog_file_path):
        print("⚠️ changelog.txt 파일을 생성하여 작성해주세요.")
    
    os.startfile(changelog_file_path)  # 파일 자동 열기
    input("👉 작성 완료 후 엔터를 누르세요...")
    
    # 파일 내용 읽기
    with open(changelog_file_path, 'r', encoding='utf-8') as f:
        changelog_content = f.read().strip()
    
    # 2. Release 데이터 구성
    release_data = {
        "tag_name": tag_name,                    # v1.1.0
        "target_commitish": "main",              # 브랜치
        "name": f"Release {version}",            # Release 1.1.0
        "body": f"## 변경사항\n\n{changelog_content}\n\n**자체 업데이트 가능 버전**",
        "draft": False,                          # 즉시 공개
        "prerelease": False                      # 정식 릴리즈
    }
    
    # 3. GitHub API 호출 (Release 생성)
    headers = {
        "Authorization": f"token {token}",
        "Accept": "application/vnd.github.v3+json"
    }
    
    url = f"https://api.github.com/repos/{repo_owner}/{repo_name}/releases"
    response = requests.post(url, json=release_data, headers=headers)
    
    if response.status_code != 201:
        print(f"❌ 릴리즈 생성 실패: {response.status_code}")
        print(response.text)
        return False
    
    release_id = response.json()['id']
    print(f"✅ 릴리즈 생성 완료 (ID: {release_id})")
    
    # 4. ZIP 파일 업로드
    upload_url = f"https://uploads.github.com/repos/{repo_owner}/{repo_name}/releases/{release_id}/assets"
    
    with open(zip_path, 'rb') as f:
        upload_headers = {
            "Authorization": f"token {token}",
            "Content-Type": "application/zip"
        }
        params = {"name": zip_path.name}
        
        response = requests.post(
            upload_url, 
            headers=upload_headers, 
            params=params, 
            data=f
        )
    
    if response.status_code != 201:
        print(f"❌ 파일 업로드 실패: {response.status_code}")
        return False
    
    print(f"✅ 파일 업로드 완료: {zip_path.name}")
    return True
```

**GitHub API 엔드포인트:**
- Release 생성: `POST /repos/{owner}/{repo}/releases`
- Asset 업로드: `POST /repos/{owner}/{repo}/releases/{id}/assets`

#### 1.4 Slack 알림 (선택사항)
```python
def send_slack_notification(version, changelog, webhook_url):
    """Slack Webhook으로 릴리즈 알림 전송"""
    message = {
        "text": f":rocket: *BundleEditor {version}* 업데이트\n"
                f"• 업데이트 방법: 앱 재실행 또는 Help-업데이트 확인\n"
                f"• 변경사항: {changelog}\n"
    }
    
    response = requests.post(webhook_url, json=message)
    
    if response.status_code == 200:
        print("✅ Slack 알림 전송 성공")
    else:
        print(f"⚠️ Slack 알림 실패: {response.status_code}")
```

#### 1.5 Webhook 선택 UI
```python
def choose_webhook(webhooks: dict) -> str:
    """여러 Webhook 중 선택"""
    keys = list(webhooks.keys())
    print("\n🔔 사용할 Slack Webhook을 선택하세요:")
    for i, k in enumerate(keys, 1):
        print(f"{i}. {k}")
    
    while True:
        try:
            choice = int(input("번호 입력 (취소=0): ").strip())
            if choice == 0:
                return None
            elif 1 <= choice <= len(keys):
                selected_key = keys[choice - 1]
                return webhooks[selected_key]
            else:
                print("⚠️ 잘못된 번호입니다.")
        except ValueError:
            print("⚠️ 숫자를 입력해주세요.")
```

### 2. 실행 흐름 (main 함수)

```python
def main():
    print("=" * 60)
    print("BundleEditor 로컬 빌드 배포 스크립트")
    print("=" * 60)
    
    # 1. 버전 정보 로드
    version_info = load_version_info()
    if not version_info:
        return 1
    
    version = version_info.get("version")
    changelog = version_info.get("changelog", [{}])[0].get("changes", ["자동 배포"])[0]
    
    print(f"버전: {version}")
    print(f"변경사항: {changelog}")
    
    # 2. ZIP 패키지 생성
    zip_path = create_zip_package()
    if not zip_path:
        print("❌ ZIP 패키지 생성 실패!")
        return 1
    
    # 3. 배포 확인
    print(f"\n🚀 {version} 릴리즈를 GitHub에 배포하시겠습니까?")
    response = input("계속하려면 'y'를 입력하세요: ").lower().strip()
    if response != 'y':
        print("배포 취소됨")
        return 0
    
    # 4. GitHub 토큰 가져오기
    token = get_github_token()
    if not token:
        return 1
    
    # 5. Slack Webhook 로드 (선택사항)
    token_data = load_token_data()
    webhooks = {k: v for k, v in (token_data or {}).items() 
                if k.startswith("webhook_")}
    
    try:
        # 6. GitHub Release 생성 및 업로드
        if not create_github_release(version, changelog, token, zip_path):
            return 1
        
        print("\n" + "=" * 60)
        print("✅ GitHub 릴리즈 완료!")
        print("=" * 60)
        print(f"릴리즈 URL: https://github.com/SungMinseok/PbbAuto/releases/tag/{version}")
        
        # 7. Slack 알림 (선택사항)
        if webhooks:
            webhook_url = choose_webhook(webhooks)
            if webhook_url:
                send_slack_notification(version, changelog, webhook_url)
        
        return 0
    
    except Exception as e:
        print(f"\n❌ 배포 중 오류 발생: {e}")
        import traceback
        traceback.print_exc()
        return 1
    
    finally:
        # 8. 임시 파일 정리
        cleanup_files(zip_path)
```

### 3. 주요 특징

#### 3.1 사용자 친화적 인터페이스
```
========================================
BundleEditor 로컬 빌드 배포 스크립트
========================================
버전: 1.1.0
변경사항: 자동 저장 기능 추가

[1/4] ZIP 패키지 생성 중...
✅ ZIP 패키지 생성 완료: dist/BundleEditor.zip

🚀 1.1.0 릴리즈를 GitHub에 배포하시겠습니까?
계속하려면 'y'를 입력하세요: y

[2/4] GitHub 릴리즈 생성 중... (1.1.0)
========================================
'changelog.txt' 파일을 열어 작성해주세요.
👉 준비가 되면 엔터(Enter)를 누르세요...

✅ 릴리즈 생성 완료 (ID: 12345)

[3/4] ZIP 파일 업로드 중...
✅ 파일 업로드 완료: BundleEditor.zip

[4/4] 임시 파일 정리...

========================================
✅ GitHub 릴리즈 완료!
========================================
릴리즈 URL: https://github.com/SungMinseok/PbbAuto/releases/tag/1.1.0

🔔 사용할 Slack Webhook을 선택하세요:
1. webhook_team1
2. webhook_team2
번호 입력 (취소=0): 1

✅ Slack 알림 전송 성공
```

#### 3.2 오류 처리
```python
try:
    if not create_github_release(version, changelog, token, zip_path):
        return 1
except Exception as e:
    print(f"❌ 배포 중 오류 발생: {e}")
    import traceback
    traceback.print_exc()
    return 1
finally:
    cleanup_files(zip_path)  # 실패해도 정리
```

---

## 버전 관리 시스템

### version.json 구조

```json
{
  "version": "1.1.0",
  "build_date": "2025-12-09",
  "update_url": "https://api.github.com/repos/SungMinseok/PbbAuto/releases/latest",
  "changelog": [
    {
      "version": "1.1.0",
      "date": "2025-12-09",
      "changes": [
        "Semantic Versioning 전환",
        "버전 타입 선택 기능 추가",
        "자동 업데이트 시스템 개선"
      ]
    },
    {
      "version": "1.0.0",
      "date": "2025-10-29",
      "changes": [
        "첫 안정 버전 릴리즈",
        "주요 기능 구현 완료"
      ]
    }
  ]
}
```

### 필드 설명

| 필드 | 타입 | 설명 |
|------|------|------|
| `version` | string | 현재 버전 (SemVer 형식) |
| `build_date` | string | 빌드 날짜 (YYYY-MM-DD) |
| `update_url` | string | GitHub API 최신 릴리즈 URL |
| `changelog` | array | 버전별 변경사항 목록 |
| `changelog[].version` | string | 해당 버전 번호 |
| `changelog[].date` | string | 릴리즈 날짜 |
| `changelog[].changes` | array | 변경사항 리스트 |

### 자동 업데이트 시스템 연동

애플리케이션에서 `version.json`의 `update_url`을 사용하여 최신 버전 확인:

```python
# main.py 또는 updater.py에서
def check_for_updates():
    """GitHub에서 최신 버전 확인"""
    with open('version.json', 'r', encoding='utf-8') as f:
        version_info = json.load(f)
    
    current_version = version_info['version']
    update_url = version_info['update_url']
    
    # GitHub API 호출
    response = requests.get(update_url)
    latest_release = response.json()
    
    latest_version = latest_release['tag_name']  # "1.2.0"
    
    if compare_versions(current_version, latest_version) < 0:
        # 업데이트 가능
        download_url = latest_release['assets'][0]['browser_download_url']
        return True, latest_version, download_url
    
    return False, current_version, None
```

---

## 통합 워크플로우

### 1. 수동 실행 (권장)

```bash
# Step 1: 빌드
python build.py
# → 버전 타입 선택 (PATCH/MINOR/MAJOR)
# → 변경사항 입력
# → EXE 생성

# Step 2: Git 커밋
git add .
git commit -m "feat: 새 기능 추가"
git push origin main

# Step 3: GitHub 배포
python deploy_local.py
# → changelog.txt 작성
# → GitHub Release 생성
# → Slack 알림 (선택)
```

### 2. 배치 파일 자동화 (deploy.bat)

```batch
@echo off
echo ========================================
echo PbbAuto Local Build Deploy Script
echo ========================================

REM 1. 버전 업데이트
echo [1/6] Update version.json...
python update_version.py patch "버그 수정"

REM 2. 빌드 (버전 업데이트 건너뛰기)
echo [2/6] Local Build (EXE)...
set SKIP_VERSION_UPDATE=1
python build.py

REM 3. Git 상태 확인
echo [3/6] Check local changes...
git status

REM 4. Git 커밋
echo [4/6] Commit changes...
git add .
git commit -m "Local Build %VERSION%"

REM 5. Git 푸시
echo [5/6] Push to main...
git push origin main

REM 6. GitHub 배포
echo [6/6] Deploy to GitHub Releases...
python deploy_local.py

pause
```

### 3. 워크플로우 다이어그램

```
┌─────────────────┐
│  버전 선택      │
│  (PATCH/MINOR/  │
│   MAJOR)        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ version.json    │
│ 업데이트        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Windows 버전    │
│ 파일 생성       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ PyInstaller     │
│ Spec 생성       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ EXE 빌드        │
│ (PyInstaller)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ ZIP 패키지 생성 │
│ (EXE + version) │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ changelog.txt   │
│ 작성            │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ GitHub Release  │
│ 생성            │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ ZIP 파일 업로드 │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Slack 알림      │
│ (선택사항)      │
└─────────────────┘
```

---

## 다른 프로젝트 적용 가이드

### 1. 필수 파일 목록

```
프로젝트/
├── build.py              # 이 문서의 코드 참조
├── deploy_local.py       # 이 문서의 코드 참조
├── update_version.py     # (선택) 버전만 업데이트
├── version.json          # 초기 버전 정보
├── token.json            # GitHub 토큰 (gitignore 추가!)
└── deploy.bat            # (선택) Windows 자동화
```

### 2. 프로젝트별 커스터마이징

#### 2.1 build.py 수정 사항

```python
# 1. 메인 스크립트 파일명 변경
a = Analysis(
    ['main.py'],  # ← 프로젝트의 메인 파일
    ...
)

# 2. EXE 파일명 변경
exe = EXE(
    ...
    name='YourAppName',  # ← 원하는 EXE 이름
    icon='your_icon.ico',  # ← 아이콘 파일
)

# 3. 포함할 데이터 폴더 변경
for folder in ['data', 'config', 'assets']:  # ← 프로젝트별 폴더
    if os.path.exists(folder):
        datas_list.append(f"('{folder}', '{folder}')")

# 4. Hidden imports 추가
hiddenimports=[
    'your_module',
    'another_dependency',
    ...
]
```

#### 2.2 deploy_local.py 수정 사항

```python
# 1. GitHub 저장소 정보 변경
repo_owner = "YourUsername"  # ← GitHub 사용자명
repo_name = "YourRepo"       # ← 저장소 이름

# 2. ZIP 파일명 변경
zip_path = dist_dir / "YourApp.zip"  # ← 원하는 ZIP 이름

# 3. EXE 파일명 변경
exe_file = dist_dir / "YourApp.exe"  # ← build.py의 name과 동일

# 4. Slack 메시지 커스터마이징
message = {
    "text": f":rocket: *YourApp {version}* 업데이트\n"
            f"• 변경사항: {changelog}\n"
}
```

### 3. token.json 설정

```json
{
  "github_token": "ghp_your_token_here",
  "webhook_team1": "https://hooks.slack.com/services/xxx/yyy/zzz",
  "webhook_team2": "https://hooks.slack.com/services/aaa/bbb/ccc"
}
```

**GitHub 토큰 생성:**
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. 권한 선택: `repo` (전체)
4. 토큰 복사하여 token.json에 저장

**⚠️ 중요: `.gitignore`에 추가**
```gitignore
token.json
*.zip
dist/
build/
```

### 4. version.json 초기 설정

```json
{
  "version": "1.0.0",
  "build_date": "2025-01-01",
  "update_url": "https://api.github.com/repos/YourUsername/YourRepo/releases/latest",
  "changelog": [
    {
      "version": "1.0.0",
      "date": "2025-01-01",
      "changes": [
        "첫 릴리즈"
      ]
    }
  ]
}
```

### 5. 의존성 패키지

**필수 패키지:**
```bash
pip install pyinstaller requests pywin32
```

또는 `requirements.txt`:
```txt
pyinstaller>=6.0.0
requests>=2.31.0
pywin32>=306
```

**⚠️ 중요: pywin32 설치 필수**
- PyInstaller가 Windows 환경에서 빌드할 때 `win32com` 관련 hook을 처리하기 위해 필요
- 설치하지 않으면 빌드 중 `ModuleNotFoundError: No module named 'win32com'` 오류 발생
- 설치 후에도 오류가 발생하면 가상환경을 재생성하거나 다음 명령 실행:
  ```bash
  python Scripts/pywin32_postinstall.py -install
  ```

### 6. 테스트 체크리스트

- [ ] `python build.py` 실행 → EXE 생성 확인
- [ ] `dist/YourApp.exe` 실행 → 정상 동작 확인
- [ ] `python deploy_local.py` 실행 → GitHub Release 생성 확인
- [ ] GitHub에서 ZIP 다운로드 → 압축 해제 후 실행
- [ ] 자동 업데이트 기능 테스트 (앱에서)

### 7. 문제 해결

#### Q1: ModuleNotFoundError: No module named 'win32com'
```bash
# 증상: PyInstaller 빌드 중 타임아웃 또는 오류 발생
# 해결: pywin32 패키지 설치
pip install pywin32

# 가상환경 사용 시
python -m pip install pywin32

# 설치 후 post-install 실행 (필요 시)
python Scripts/pywin32_postinstall.py -install
```

**원인:**
- PyInstaller가 Windows 환경에서 `win32com` hook을 처리하려 시도
- `pywin32`가 설치되지 않으면 hook 분석 중 오류 발생

**대안 (pywin32 없이 빌드):**
```python
# spec 파일의 excludes에 추가
excludes=[
    'win32com',
    'win32api',
    'win32con',
    'pythoncom',
    'pywintypes',
]
```

#### Q2: PyInstaller 빌드 실패
```bash
# 해결: 명시적으로 hiddenimports 추가
hiddenimports=[
    'your_missing_module',
]
```

#### Q3: GitHub API 403 오류
```
해결: GitHub 토큰 권한 확인
- repo 권한 필요
- 토큰이 만료되지 않았는지 확인
```

#### Q4: ZIP 파일에 version.json이 없음
```python
# 해결: create_zip_package() 함수 확인
zipf.write(version_json_path, 'version.json')  # 경로 확인
```

#### Q5: 버전이 업데이트되지 않음
```python
# 해결: version.json 파일 권한 확인
# Windows: 읽기 전용 속성 해제
```

#### Q6: 빌드 폴더 삭제 실패 (PermissionError)
```
증상: build/ 또는 dist/ 폴더 삭제 중 권한 오류
해결: 
1. 빌드된 EXE 파일이 실행 중이면 종료
2. 바이러스 검사 프로그램 일시 중지
3. 빌드는 정상 진행됨 (폴더 삭제 실패해도 PyInstaller가 덮어씀)
```

### 8. 고급 기능 추가

#### 8.1 다중 플랫폼 지원
```python
# build.py에 추가
import platform

if platform.system() == 'Windows':
    exe_name = 'YourApp.exe'
    icon_file = 'app.ico'
elif platform.system() == 'Darwin':  # macOS
    exe_name = 'YourApp'
    icon_file = 'app.icns'
else:  # Linux
    exe_name = 'YourApp'
    icon_file = None
```

#### 8.2 자동 Git 태그 생성
```python
# deploy_local.py의 main()에 추가
def create_git_tag(version):
    """Git 태그 자동 생성"""
    tag_name = f"v{version}"
    
    # 태그 생성
    subprocess.run(['git', 'tag', '-a', tag_name, '-m', f'Release {version}'])
    
    # 태그 푸시
    subprocess.run(['git', 'push', 'origin', tag_name])
    
    print(f"✅ Git 태그 생성 완료: {tag_name}")

# main() 함수에서 호출
if create_github_release(...):
    create_git_tag(version)
```

#### 8.3 릴리즈 노트 자동 생성
```python
def generate_release_notes(version_info):
    """version.json의 changelog에서 릴리즈 노트 생성"""
    changelog = version_info.get('changelog', [])
    if not changelog:
        return "변경사항 없음"
    
    latest = changelog[0]
    changes = latest.get('changes', [])
    
    notes = "## 변경사항\n\n"
    for change in changes:
        notes += f"- {change}\n"
    
    return notes

# create_github_release()에서 사용
release_notes = generate_release_notes(version_info)
release_data['body'] = release_notes
```

---

## 요약

### build.py의 핵심
1. **버전 관리**: Semantic Versioning 자동 증가
2. **EXE 빌드**: PyInstaller를 사용한 단일 파일 생성
3. **유연성**: 환경변수로 동작 커스터마이징

### deploy_local.py의 핵심
1. **패키징**: EXE + version.json → ZIP
2. **배포**: GitHub Release API 사용
3. **알림**: Slack Webhook 통합

### 적용 시 핵심 포인트
1. 프로젝트별 파일명/경로 커스터마이징
2. GitHub 토큰 안전하게 관리
3. version.json 구조 유지
4. 자동 업데이트 시스템과 연동

---

**문서 버전**: 1.0.0  
**작성일**: 2025-12-09  
**라이선스**: MIT

