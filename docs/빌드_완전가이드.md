# 🚀 QuickBuild 빌드 완전 가이드

## ✅ 완료된 수정 사항

### 1. requirements.txt 대대적 정리 ✅
- ❌ 이전: 169개 패키지 (불필요한 패키지 다수 포함)
- ✅ 현재: 11개 필수 패키지만 포함
- ✅ PyInstaller 5.13.2 (안정 버전)
- ✅ PyAutoGUI 0.9.54 (최신 버전)

### 2. 인코딩 문제 해결 ✅
- ✅ UTF-8 인코딩 명시
- ✅ 로그 파일 저장 방식으로 변경
- ✅ 실시간 출력 필터링

### 3. 빌드 스크립트 개선 ✅
- ✅ `build_release.py` - 완전 개선
- ✅ `quick_build.bat` - 원클릭 빌드
- ✅ `setup_clean_env.bat` - 클린 환경 설정
- ✅ `check_before_build.py` - 빌드 전 체크

## 📦 새 requirements.txt

```
# QuickBuild 필수 패키지

# GUI 프레임워크
PyQt5>=5.15.0

# 웹 자동화
selenium>=4.0.0
chromedriver-autoinstaller>=0.6.0

# HTTP 요청
requests>=2.31.0

# 유틸리티
colorama>=0.4.6
tqdm>=4.64.1
packaging>=21.0

# 빌드 도구
pyinstaller==5.13.2
setuptools>=65.0.0

# 자동화 (필요 시)
PyAutoGUI==0.9.54
```

## 🛠️ 빌드 방법 (단계별)

### Step 1: 클린 환경 설정

```bash
setup_clean_env.bat
```

이 스크립트가 자동으로:
1. 가상환경 활성화
2. pip 업그레이드
3. 필수 패키지 설치
4. 설치 확인

### Step 2: 빌드 전 체크

```bash
python check_before_build.py
```

다음 항목을 자동으로 체크:
- ✅ Python 버전
- ✅ 필수 패키지 설치 여부
- ✅ 필수 파일 존재 여부
- ✅ spec 파일 구성

### Step 3: 빌드 실행

```bash
quick_build.bat
```

또는

```bash
python build_release.py
```

## 📁 생성되는 파일

### 성공 시:
```
dist/
  └─ QuickBuild_3.0-25.10.27.1635.zip
     ├─ QuickBuild.exe (단일 실행 파일)
     └─ version.json
```

### 실패 시:
```
build_pyinstaller.log  (상세 로그 확인)
```

## 🔍 트러블슈팅

### 문제 1: 패키지 설치 오류

**증상:**
```
No module named 'xxx'
```

**해결:**
```bash
# 가상환경 활성화
C:\pyenvs\getbuild\Scripts\activate

# 필수 패키지 재설치
pip install -r requirements.txt --upgrade
```

### 문제 2: PyInstaller 인코딩 오류

**증상:**
```
UnicodeDecodeError: 'cp949' codec can't decode...
```

**해결:**
✅ 이미 수정됨! `build_release.py`에서 UTF-8 인코딩 처리 완료

### 문제 3: 빌드는 되는데 실행 안됨

**확인사항:**
1. `version.json` 파일이 프로젝트 루트에 있는지 확인
2. `qss/` 폴더가 있는지 확인
3. `ico.ico` 파일이 있는지 확인

**해결:**
```bash
# 빌드 전 체크 실행
python check_before_build.py
```

### 문제 4: Windows Defender 경고

**증상:**
빌드된 EXE 파일이 삭제되거나 실행 안됨

**해결:**
1. Windows Defender 설정 > 바이러스 및 위협 방지
2. "제외 항목" 추가
3. `dist/` 폴더 추가

## 📝 파일 구조

### 빌드 관련 파일:
```
GetBuild_clean/
├─ build_release.py         # 메인 빌드 스크립트
├─ quick_build.bat           # 원클릭 빌드
├─ setup_clean_env.bat       # 환경 설정
├─ check_before_build.py     # 빌드 전 체크
├─ requirements.txt          # 필수 패키지 (11개)
├─ requirements_old.txt      # 이전 패키지 (백업)
├─ index.spec               # PyInstaller 설정
├─ QuickBuild.spec          # 대체 spec
├─ hook-pkg_resources.py    # PyInstaller hook
└─ 빌드_완전가이드.md        # 이 파일!
```

### 소스 파일:
```
├─ index.py                 # 메인 앱
├─ core/                    # 핵심 모듈
│  ├─ __init__.py
│  ├─ aws_manager.py
│  ├─ config_manager.py
│  ├─ scheduler.py
│  ├─ build_operations.py
│  └─ worker_thread.py
├─ ui/                      # UI 모듈
│  ├─ __init__.py
│  ├─ schedule_dialog.py
│  └─ schedule_item_widget.py
├─ makelog.py
├─ exporter.py
└─ updater.py
```

## 🎯 빌드 순서 (요약)

```
1. setup_clean_env.bat      ← 환경 설정 (최초 1회)
2. python check_before_build.py  ← 빌드 전 체크
3. quick_build.bat           ← 빌드 실행
```

## 💡 Tips

### Tip 1: 빠른 재빌드
이미 dist/ 폴더가 있으면 빌드를 건너뜁니다.
강제 재빌드하려면:
```bash
rd /s /q dist build
python build_release.py
```

### Tip 2: 로그 확인
빌드 중 문제가 생기면:
```bash
# 로그 파일 열기
notepad build_pyinstaller.log
```

### Tip 3: 개발 모드
빌드 없이 바로 실행하려면:
```bash
python index.py
```
창 제목에 `[개발용]`이 표시됩니다.

## 🔄 업데이트 히스토리

### 2025-10-27
- ✅ requirements.txt 대대적 정리 (169개 → 11개)
- ✅ PyInstaller 5.13.2로 업데이트
- ✅ 인코딩 오류 완전 해결
- ✅ 빌드 스크립트 개선
- ✅ 빌드 전 체크 스크립트 추가
- ✅ 클린 환경 설정 스크립트 추가

## 📞 문제 해결 순서

1. **`check_before_build.py` 실행** - 환경 체크
2. **`build_pyinstaller.log` 확인** - 오류 로그 확인
3. **가상환경 재설정** - `setup_clean_env.bat`
4. **패키지 재설치** - `pip install -r requirements.txt --upgrade`

## ✨ 성공 메시지

빌드가 성공하면:
```
========================================
✅ 빌드 완료!
========================================
📦 버전: 3.0-25.10.27.1635
📁 출력 파일: dist/QuickBuild_3.0-25.10.27.1635.zip
🗑️  임시 파일: 정리됨 (build/, *.spec, version_info.txt)

다음 단계:
  python deploy_github.py  # GitHub Release 배포
========================================
```

## 🎉 완료!

이제 `quick_build.bat`을 실행하면 모든 것이 자동으로 처리됩니다!

**Happy Building! 🚀**



